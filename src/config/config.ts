export const COLS = parseInt(import.meta.env.VITE_COLS || '130', 10);
export const ROWS = parseInt(import.meta.env.VITE_ROWS || '60', 10);
export const CELL_SIZE = parseInt(import.meta.env.VITE_CELL_SIZE || '60', 10);